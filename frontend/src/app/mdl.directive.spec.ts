import { MdlDirective } from './mdl.directive';

describe('MdlDirective', () => {
  it('should create an instance', () => {
    const directive = new MdlDirective();
    expect(directive).toBeTruthy();
  });
});
